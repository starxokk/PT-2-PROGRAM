#ifndef HELPER_H  // IF this header "HELPER_H" has not been defined before
#define HELPER_H  // THEN DEFINE IT.

void showMessage(); // We declare this function showMessage() so that other cpp files can implement it.
int addition(int x, int y); // We declare this integer function addition() so that other cpp files can implement

#endif // end the if header guard.